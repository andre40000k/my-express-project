<!DOCTYPE html>
<html>

<head>
    <title>List of articles</title>
</head>

<body>
    <h1>All articles</h1>
    <ul>
        <% articles.forEach(article=> { %>
            <li>
                <a href="/articles/<%= article.id %>">
                    <%= article.title %>
                </a>
            </li>
            <% }) %>
    </ul>
</body>

</html>